export type Hotel = {
    location_id: string,
    hotel_name: string,
    hotel_lat: string,
    hotel_lng: string,
    photo_url: string,
    hotel_price: number,
    hotel_rating: number,
    hotel_address: string
};