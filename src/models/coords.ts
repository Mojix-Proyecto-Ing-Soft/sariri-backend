export type Coords = {
    bl_latitude: number;
    bl_longitude: number;
    tr_latitude: number;
    tr_longitude: number;
    max_places?: number;
};

export type PointCoords = {
    lat: number;
    lng: number;
};